<header>
    <nav>
        <a href="">EDWARD BYRNE</a>
        <button class="hamb" aria-label="Open Menu"><svg class="ham" viewBox="0 0 100 100"><path class="line top" d="m 30,33 h 40 c 3.722839,0 7.5,3.126468 7.5,8.578427 0,5.451959 -2.727029,8.421573 -7.5,8.421573 h -20"></path><path class="line middle" d="m 30,50 h 40"></path><path class="line bottom" d="m 70,67 h -40 c 0,0 -7.5,-0.802118 -7.5,-8.365747 0,-7.563629 7.5,-8.634253 7.5,-8.634253 h 20"></path></svg><span>MENU</span></button>
        <div class="nav-links-wrapper">
        
                <a href="">HOME</a>
                <a href="">PROJECTS</a>
                <a href="">ABOUT</a>
                <a href="">CONTACT</a>
           
            
        </div>
    </nav>

</header>

<script>
    const span = document.querySelector('span');
    const navLinks = document.querySelector('.nav-links-wrapper');
    const button = document.querySelector('.hamb');
    button.addEventListener('click',()=>{
        button.classList.toggle('active')
        navLinks.classList.toggle('visible')
       button.classList.contains('active') ? span.innerHTML = 'CLOSE' : span.innerHTML = 'MENU'
    console.log(button.innerHTML);
    
    })
    
    </script>


<style lang="scss">
    header {
        padding: 0 2rem;
    }
    nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        
        a {
            font-size: 1.25rem;
            color: white;
            text-decoration: none;
        };
    }

    .nav-links-wrapper {
        display: grid;
        place-content: center;
        gap: 4rem;
        padding: 3rem;
        overflow: hidden;
        position: fixed;
        width: 25vw;
        z-index: 99;
        right: -100%;
        top: 0;
        transition: all .5s ease;
        background-color: #221e27;
        color: white;
        height: 100%;
        a {
            font-size: 5.5rem;
        }
    }

    .visible {
        right: 0;
        opacity: 1;
        overflow: hidden;
        top: 0;
        transition: all .5s ease;
        
    }

    
   button{
    display: flex;
    align-items: center;
    color:white;
    font-weight: bold;
    font-size: 1.2rem;
    cursor: pointer;
    z-index: 999;
}
.hamb {
    position: relative;
    margin-right: -.625rem;
    border-width: 0px;
    background-color: transparent;
    padding: 0;
}
.sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0,0,0,0);
    white-space: nowrap;
    border-width: 0;
}
.hamb .ham {
    -webkit-tap-highlight-color: transparent;
    -webkit-user-select: none;
    -moz-user-select: none;
    user-select: none;
}
.hamb .ham {
    height: 60px;
    width: 60px;
    transition-duration: .3s;
}
.hamb .ham .top {
    stroke-dasharray: 40 160;
}
.hamb .ham .middle {
    transform-origin: 50%;
    stroke-dasharray: 40 142;
}
.hamb .ham .bottom {
    transform-origin: 50%;
    stroke-dasharray: 40 85;
}
.hamb .line {
    fill: none;
    stroke: rgb(255, 255, 255);
    stroke-width: 5;
    transition-duration: .3s;
    stroke-linecap: round;
}
.hamb.active svg {
    --tw-rotate: 45deg;
    transform: rotate(var(--tw-rotate));
}
.hamb.active svg .top {
    stroke-dashoffset: -64px;
}
.hamb.active svg .middle {
    --tw-rotate: 90deg;
    transform: rotate(var(--tw-rotate));
}
.hamb.active svg .bottom {
    stroke-dashoffset: -64px;
}


</style>